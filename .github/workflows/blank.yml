name: Jaime-DataCollection

permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
    inputs:
      dry-run:
        type: boolean
        default: false
      environment:
        type: choice
        options:
          - development
          - staging
          - production

  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
    branches:
      - main
      - develop/**
      - release/**
      - hotfixes/**

env:
  IS_DEV: ${{ (inputs.dry-run && inputs.environment == 'development')
      || (!inputs.dry-run && startsWith(github.ref, 'refs/heads/develop/')) }}

  IS_STAGING: ${{ (inputs.dry-run && inputs.environment == 'staging')
      || (!inputs.dry-run && startsWith(github.ref, 'refs/heads/release/'))
      || (!inputs.dry-run && startsWith(github.ref, 'refs/heads/hotfixes/')) }}

  IS_PROD: ${{ (inputs.dry-run && inputs.environment == 'production')
      || (!inputs.dy-run && github.ref_type == 'tag') }}

jobs:
  detect_changes:
    name: Detect Environment + Changed Components
    runs-on: ubuntu-latest

    outputs:
      environment: ${{ steps.set-env.outputs.environment }}
      workflows: ${{ steps.filter.outputs.workflows }}
      skip_sf: ${{ steps.filter.outputs.skip_sf }}

    steps:
      - uses: actions/checkout@v4

      - id: set-env
        run: |
          if [ "${{ env.IS_DEV }}" = "true" ]; then
            echo "environment=dev" >> $GITHUB_OUTPUT
          elif [ "${{ env.IS_STAGING }}" = "true" ]; then
            echo "environment=stg" >> $GITHUB_OUTPUT
          elif [ "${{ env.IS_PROD }}" = "true" ]; then
            echo "environment=prod" >> $GITHUB_OUTPUT
          else
            echo "::error::Cannot determine environment"
            exit 1
          fi

      - id: changes
        uses: tj-actions/changed-files@v46.0.1
        with:
          dir_names: true
          files: |
            step-function-workflow/**

      - id: filter
        run: |
          ENV="${{ steps.set-env.outputs.environment }}"
          RAW="${{ steps.changes.outputs.all_changed_files }}"

          FILES=$(printf '%s\n' "$RAW" | jq -R -s -c 'split("\n") | map(select(. != ""))')

          # Only JSON files inside data-collection
          WORKFLOWS=$(echo "$FILES" | jq -c "[.[] 
            | select(startswith(\"step-function-workflow/${ENV}/data-collection\")) 
            | select(endswith(\".json\"))]")

          echo "workflows=$WORKFLOWS" >> $GITHUB_OUTPUT

          # ONLY skip Step Function update step, NOT deploy job
          if [[ "$WORKFLOWS" == "[]" ]]; then
            echo "skip_sf=true" >> $GITHUB_OUTPUT
          else
            echo "skip_sf=false" >> $GITHUB_OUTPUT
          fi

  deploy:
    needs: detect_changes
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: |
          echo "Environment: ${{ needs.detect_changes.outputs.environment }}"
          echo "Workflows:   ${{ needs.detect_changes.outputs.workflows }}"
          echo "Skip SF Update: ${{ needs.detect_changes.outputs.skip_sf }}"

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::227295996532:role/step-function-role2285
          aws-region: ap-northeast-1

      # ONLY THIS STEP SKIPS — NOT THE WHOLE JOB
      - name: Update Step Functions directly
        if: ${{ needs.detect_changes.outputs.skip_sf == 'false' }}
        run: |
          echo '${{ needs.detect_changes.outputs.workflows }}' \
            | jq -r '.[]' | while read STATE_FILE; do

                WF_NAME=$(basename "$STATE_FILE" .json)
                ENV="${{ needs.detect_changes.outputs.environment }}"
                STATE_MACHINE_NAME="${WF_NAME}-${ENV}"

                echo "Updating $STATE_MACHINE_NAME using $STATE_FILE"

                jq empty "$STATE_FILE"

                aws stepfunctions update-state-machine \
                  --state-machine-arn "arn:aws:states:ap-northeast-1:227295996532:stateMachine:${STATE_MACHINE_NAME}" \
                  --definition "file://${STATE_FILE}"

                echo "Updated → $STATE_MACHINE_NAME"
            done
